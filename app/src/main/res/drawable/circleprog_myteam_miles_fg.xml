<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/progress">
        <shape
            android:innerRadius="@dimen/circleprog_myteam_miles_radius"
            android:shape="ring"
            android:thickness="@dimen/circleprog_myteam_miles_thickness">
            <gradient
                android:centerColor="@color/color_light_green"
                android:endColor="@color/color_dark_green"
                android:startColor="@color/color_yellow"
                android:type="sweep" />
        </shape>
    </item>

</layer-list>